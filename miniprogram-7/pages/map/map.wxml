<view class="map-container">
      <text class="title">联系人位置</text>
      <map
        id="map"
        longitude="{{longitude}}"
        latitude="{{latitude}}"
        scale="14"
        markers="{{markers}}"
        show-location="true"
        style="width: 100%; height: 500px;"
      ></map>
      <text class="tip">{{tip}}</text>
    </view>